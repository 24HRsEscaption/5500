interface Animal {
  name: String;
  age: number;

  // break rule no.5: the rule of meaningful interface, should add the following methods
  // getAnimalInfo(): string;
  // getAnimalSound(): string;
}